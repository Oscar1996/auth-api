version: '3.8'
services: 
  api:
    build:
      dockerfile: Dockerfile.dev 
      context: ./api
    restart: always
    volumes: 
      - /app/node_modules
      - ./api:/app
    ports:
      - '8080:8080'